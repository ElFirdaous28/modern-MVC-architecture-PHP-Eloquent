# Ignore vendor directory (Composer dependencies)
/vendor/

# Ignore Composer's lock file (if you don’t want to track specific versions)
composer.lock

# ignore the config
/config/config.php

# ignore the .nev file
.env